# Override para produção — usado pelo CI/CD junto com docker-compose.yml
# Uso: docker compose -f docker-compose.yml -f docker-compose.prod.yml <comando>
services:
  api:
    image: ghcr.io/vulpeslari/boto-tts-api:latest
